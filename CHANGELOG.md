# 更新记录

## v1.5.3 — 2026-09-25

**出稿后加「事实核查」一关：逐条用更多信源核实，高危没处置完不许推**

孙哥提的：「每次出新闻之后，再用更多的信源来核实新闻内容的可靠性。尤其是数据、趋势类的新闻，
确保完全可靠再推。有些无法自己判断的，要输出判断指令给作者。」

此前技能里**只有天气**做了多源验证（v1.5.0，同一份数据 7 源取中位数），
新闻正文一直是「搜到什么写什么」，**没有任何核实环节** —— 本版补上。

### 1. 新增 `scripts/fact_check.py`（两步走）

```bash
python scripts/fact_check.py --content 39_content.json            # 1) 抽取待核清单
python scripts/fact_check.py --content 39_content.json --report    # 2) 出报告 + 过卡口
```

- **抽取**：机械扫正文，命中「数字+单位 / 百分比幅度 / 趋势 / 绝对化 / 时间 / 期限生效日 /
  事实性事件 / 引用预测 / 标准法规编号」就抽出来，并给每条生成建议核查查询
- **分级**：数据+趋势、数据+绝对化、带百分比、金额规模份额类、标准编号、期限生效日、
  **头条里的数字** → **高**；有数字 / 引用 / 时间 / 事件 → **中**；纯定性 → **低**
- **产物**：`factcheck_<name>.json`（回填载体，唯一要手改的）·`核查清单_<name>.md`·
  `事实核查报告_<name>.html`
- **卡口**：高危仍有 `pending`/`unjudgeable` → 退出码 **2**（先别推）；中危 `pending` 只提醒
- 报告是**单文件**（无外链资源），手机直接能看；「给作者的判断指令」单独成区

### 2. 新增 `assets/sources.json`（信源分级表）与 `references/fact-check.md`（核查规范）

- 信源分 **A 一手官方 / B 权威媒体垂媒 / C 自媒体聚合** 三级；
  确立「**≥2 个相互独立的来源、且至少 1 个 A/B 级**才算确证」，
  「**同一篇被多家转载不算多源**，必须回溯首发」
- 写死几条判据：数据必须带统计期（「最新数据显示」而无日期 = 不通过）·
  口径陷阱清单（同比/环比、销售额/销量/GMV、线上/全渠道、**高增长要看基数**）·
  标准法规要核生效日 · 公司经营数据一律 `unjudgeable`（外部永远查不到）
- 给出**弱化方向**（查不实但值得保留时怎么改）与**判断指令句式**
  （`【要你定夺】` + 为什么判不了 + 2~3 个可选处置）
- 无人值守（定时任务）策略：照跑核查，核不实的一律弱化或删掉，
  交付时列出「核实了 N 条，其中 M 条已弱化/删除」并留回退口

### 3. `build.py` 加核查提醒（**只提醒，不改退出码**）

出图跑完会顺带打印本期核查状态（没做核查 / 高危未处置 / 中危待核）。
**故意不做阻断** —— 定时任务没人应答时，不能让核查把出图卡死。

### 4. 流程与文档

- `SKILL.md` 路径 B 由 8 步变 9 步，在「写 content.json」与「生成配图」之间插入核查关
  （放这儿是为了让核查引起的改稿不用重出图）；新增第三节「事实核查」；
  文件地图、坑位清单、frontmatter description、定时任务说明同步更新
- `README.md` 新增「3. 事实核查」步骤与常见问题一条，目录结构与版本号同步
- `.gitignore` 补 `factcheck_*.json` / `核查清单_*.md` / `事实核查报告_*.html`

### 实测（用第 39 期内容跑，且真的去查了 5 条）

18 条可核查断言（高 10 / 中 8）。真实核查结果：

| 断言 | 结论 | 依据 |
|---|---|---|
| 1—8 月快递业务量 1340.6 亿件 / +4.6%、收入 10289.2 亿元 / +7.4% | **确证** | 国家邮政局官网 + 中国政府网/新华社 + 工人日报 + 人民网（4 个独立来源，逐字一致） |
| 2026 上半年母婴线上销售额 −2.1%、均价 91 元 +6.5% | **确证** | 魔镜洞察原始报告（一手）+ 中童传媒 |
| 抖音商品卡返佣：费率与 0.6%/1% 的差值、次月 20 日到账 | **确证**（名称需对齐） | 抖音电商学习中心规则原文；但官方全名是《2026年抖音电商商品卡免佣扶持政策》，文稿写成《商品卡返佣扶持政策》 |
| 四类违规「一次扣满 10 分」 | **确证**（建议补限定） | 原文另有「近 30 天累计不超过 10 分」，文稿省略了，会把严重程度说高 |
| 「9 月 8 日实施的《商家体验分规范》」 | **无法判断** | 公开报道（网经社 8/7、腾讯新闻 8/7）都说该规范是 **8 月上旬发布**；发布日 ≠ 生效日，官方生效日公开查不到 → 输出判断指令给孙哥 |

**核查确实抓到了真问题**：一条日期存疑、一条限定条件被吃掉、一条政策名称与官方不符。
第 39 期已发布、不回改；这三条已进 `SKILL.md` 坑位清单，供后续规避。

卡口实测：回填 5 条后仍剩 8 条高危 `pending` → `--report` 退出码 **2**，正确拦住交付。
报告在 390×844 手机视口渲染零横向溢出。

---

## v1.5.2 — 2026-09-25

**报头加「手机顶部开孔避让层」+ 品牌 logo 直印绿底；顺手修几处小问题**

孙哥反馈：手机上看长图时，顶部居中开孔（灵动岛 / 挖孔）压住了大标题上沿 ——
实拍里「威尔新资讯」的「新」字正好被吃掉。研究主流机型开孔几何后出了三套方案，
孙哥拍板 **方案 A2 · 避让层 + logo 直印**。

### 1. 顶部 280px 避让层（`safeTop`）

手机看长图时图片按宽度撑满，所以开孔深度换算成图高：
`图高 px = 开孔深度(pt) × 1744 ÷ 屏宽(pt)`。取最坏情况（iPhone 17/16 Pro 灵动岛 62pt → 269px）上取整 = **280px**。

- `layout.json` 新增 `safeTop.height = 206`（1280 稿宽下的 CSS px，对应最终 1744px 图上的 280px）
- 实现方式：把报头绿块的 `padding-top` **直接换成** `safeTop.height`，绿块自然长高、
  内部元素整体下移，**不新增容器层级**
- **被开孔遮住的只是一片纯色，零信息损失**
- **设 `safeTop.height = 0` 即完整退回旧版**（绿块贴顶、不渲染 headerLogo），随时可还原

### 2. 品牌 logo 直印绿底（`headerLogo`）

避让层下方居中直印一枚 logo，**不加任何底座色块**，复用页尾那同一枚 `assets/logo.png`。

- **配色用原色 `#27272a`**：在苔藓绿 `#b2b382` 上实测对比 **6.85 : 1**（过 WCAG AA 的 4.5）；
  反白只有 **2.17 : 1**，会「发飘」（先前方案里那行白色 wellber 就是靠描边硬撑的）
- `width = 646`（1280 稿宽）= 最终图上的 **880px**（占图宽 50%）
- `headerLogo.src` 留空则自动取 `assets/logo.png`（与页尾落款同一枚，保证报头/页尾两处 logo 不走样）
- 新增 `prepare_header_logo()`：渲染前把 logo 复制到产物目录，与页尾 `prepare_logo()` 同一套做法

### 3. 版式校验基准支持 `shift`（不用每期手改窗口）

- `baseline.shift = 340`：避让层造成的报头内部整体下移量。`build.py` 会自动把它加到
  `barHeight` / `titleWindow` / `navWindow` / `navBandRows` 上
  （日期是相对绿块**底部**量的，不加 shift）
- 以后调 `safeTop.height` 或 `headerLogo` 尺寸，只需重算 `shift` 一个数
- 顺手补洞：窗口里扫不到墨迹原来会**静默跳过**校验，现在会报 WARN（「窗口内无白字」）

### 4. 另外两处修复（v1.5.1 之后的零星提交，一并纳入本版）

- `count_chars.py` 支持**直接读 `content.json` 核字**（按条输出「主题词｜正标题」），不用再手贴正文
- `assets/issues.json` 台账补齐：38 期 `draft` → `published`、新增 39 期 `published`；
  并澄清 `invalidated` 里的 39 期指的是 **9/15 提前建的那份副本**，不是 39 期这个**刊期**
  （否则同一期号后来正式发布时台账会自相矛盾）

### 实测（用第 39 期内容重跑）

| | 旧版 | v1.5.2 |
|---|---|---|
| 图长 | 7045px | 7507px（+462 / +6.6%） |
| 报头绿块高 | 469px | 937px |
| 版式校验 | — | **8 项全 PASS** |
| 前 280px 非绿像素占比 | — | **0.000000** |

报头内部几何（1x 稿宽实测）：logo 行 238–310、大标题 367–568、导航词 639–669，
整体下移量精确等于 340px —— 开孔碰不到任何文字或 logo。

---

## v1.5.1 — 2026-09-15

**刊期改为「时间锚点」推算：时间没到，期号就不能 +1**

孙哥指出期号对不上：「这一期是 38 期 0918」，但脚本算出的是 **40 期 + 0918** ——
期号和日期互相打脸。根因是 `issue_info.py` 一直靠**扫项目目录取最大期号 +1**，
而目录里躺着一个误建的 39 期（0925），把推算顶到了 40。

### 1. 两条硬规则

- **规则一 · 时间锚点**：期号 = 锚点期号 + 周差。锚点写在 `assets/issues.json` 的
  `baseline` —— **38 期 = 2026-09-18（周五）**。发布日期落在哪一周就取哪一周的期号，
  差几周加几期，不再靠「上期 +1」累加。
- **规则二 · 时间没到就不能 +1**（孙哥原话）：**不管目录里做没做过第 N 期**，
  只有发布日期真的推进一周，期号才 +1。目录扫描的结果**只用来告警，不参与推算**。

覆盖顺序：`--last-issue`（手动）> 时间锚点推算 > 扫项目目录（兜底，仅当没锚点）。

### 2. 新增 `assets/issues.json`（刊期台账）

- `baseline` —— 时间锚点（38 = 2026-09-18），一般不用再动
- `records` —— 历史刊期 34~38
- `invalidated` —— 作废记录（误建的 39 期 / 测试里冒出来的 40 期），留痕防复发
- 维护命令：`python scripts/issue_info.py --add 39=2026-09-25`

### 3. `issue_info.py` 改动

- 新增 `issue_for_date(pub, anchor_issue, anchor_date)`：按各自所在周的周五算周差
- 新增 `--add` / `--status` / `--note` / `--ledger` / `--no-ledger`
- 输出新增 `time_issue` / `anchor` / `future_in_dir`；`--no-ledger` 可复现旧的扫目录行为
- 目录里出现比本期更新的期号时，只告警（「时间没到就不算数」），**不改期号**

实测：0918→38 期、0925→39 期、1002→40 期、0911→37 期；
即使目录里放着 `39_0925.png`，本周五仍稳定算 38 期。

### 4. 数据清理（已移入回收站，可还原）

误建的 **39 期全套**（`39_0925.png/pdf`、`39_0925_分片/`、`39_content.json`、`第39期.html`、
`_work/img/39_*.jpg`、`_work/39_content.json.bak`、`_work/out/39_0925-raw.png`）共 11 项；
测试产物与过期中间产物（`_v14_test/`、`_top_test/`、`_work/v15_test/`、`_work/weather_preview/`、
`_work/weather_multi/`、`_verify_*`、`_wx_*.html` 等）共 16 项。
**保留**：《天气预报多渠道验证报告_2026-09-19至25.html》与 `weather_v15/`（多源合成定稿）。

---

## v1.5.0 — 2026-09-17

**顶部加「未来一周天气」banner，7 源交叉验证后取多源中位数**

孙哥要求周报最顶部加未来一周天气：周五发布 → 窗口取**次日周六 → 下周五**共 7 天，城市**北上广**。
先出了三版 banner 预览给他挑，再做多渠道验证，最后按他拍板的 **B 方案（多源中位数）** 落地。

### 1. 新增 `scripts/weather_banner.py`

- **7 个数据源，全部免密钥**：Open-Meteo 四个模式（best_match / ECMWF / GFS / GEM）、
  中国气象局 nmc.cn、中国天气网（sojson 转发）、挪威 met.no。
- **任一源失败自动跳过**，全部失败才降级到单源 Open-Meteo，再失败就整块不渲染 ——
  **天气永远不会拖垮整期周报**。
- 三种用法：`fetch_multi()` 取数 / `render_html()` 出片段 / 命令行 `--preview` 出预览图。
- 图标是内联 SVG 线描（晴/多云/阴/雨/雷雨/雾/雪），**不依赖 emoji 字体**，跨机器不掉图。

### 2. 多源合成规则（孙哥拍板 B 方案）

- **温度 = 各源中位数**（剔除离群的 GEM 加拿大模式）
- **天气 = 各源众数**，平票时取「更轻」的那个
- **雷雨保守降级**：占比 <60% 一律写成「雨」（`THUNDER_MIN_RATIO`）
- WMO 措辞按中文习惯改过：`51/53` 不再直译成「小毛毛雨」，写「小雨」

### 3. 为什么这么麻烦 —— 7 源交叉验证的结论

2026-09-17 拿 9.19~9.25 做了次全量验证（报告见 `_work/天气预报多渠道验证报告_2026-09-19至25.html`）：

- **温度可用**：多源中位数与单源只差 ±1~2°C，21 格里 4 格高置信、2 格存疑。
- **天气词要保守**：**7 个源没有任何一天天气完全一致**。周报原来写的「北京周六有雨」
  只有 2/7 源支持，国内两个官方源都说多云。
- **越往后越不可信**：第 6、7 天各源温差 5~10°C；中国气象局只发 7 天，最后两天没有官方数据。
- **GEM 是稳定离群值**：北京夜间比别家低 8~10°C。
- **时间稳定性没问题**：9.15 版 vs 9.17 版 21 格温度零漂移（表面 13 处"变化"只是措辞归一化）。

### 4. 接进周报模板

- `layout.json` 新增 `weather` 段：`enabled` / `position`（`after_dateline` 默认，
  `after_brandbar` 可选）/ `cities` / `exclude` / 留白配色。1280 稿宽下高 565px，
  左右各留 90 与正文对齐。
- `template.html` 新增 `{{WEATHER_HTML}}` 占位。
- `build.py` 自动拉数并注入，新增 `--no-weather` / `--weather-refresh`；
  数据缓存到 `_work/weather_<起始日>.json`，重跑复用。
- 端到端实测：第 40 期测试稿 8 项版式校验**全 PASS**。

**想关掉**：`layout.json` → `weather.enabled = false`，或跑 build 时加 `--no-weather`。

## v1.4.0 — 2026-09-15

**从上到下设死四个框架：刊期必问、头条必问、条数封顶 5 条、业务边界写死**

孙哥反馈：内容不能只是"不框死"，还有几处必须**设死**。逐条落实如下。

### 1. 刊期必须确认（新增每期第一步）

- **新增 `scripts/issue_info.py`**：扫项目目录推出「上期期号 → 本期期号（+1）→ 发布日期
  （默认本周五）→ 目标文件名」，并检查**该日期是否已被别的期号占用**。
  支持 `--last-issue`（孙哥报的上期）/ `--date`（非周五发布）/ `--json`。
- `SKILL.md` 路径 B 开头插入这一步，**必须用 `AskUserQuestion` 让孙哥确认期号与日期**，
  脚本报日期冲突时**原样转达**、不许自己拍板。
- 由来：`39_content.json` 的报头曾写成 38 期 / 9.18（成品是 39 期 / 9.25），根因就是期号没当面确认过。

### 2. 第一条固定「公司头条」，每期必问

- 头条报的是公司自己的事（新品/活动/渠道/数据/团队），**AI 搜不到、也不许编**，只能问孙哥。
- **头条定稿之后**才去找后续板块 —— 它占一条配额，影响后面还能收几条。
- 头条配图**优先用公司实拍/设计图**，不要一上来就 AI 生成。
- 孙哥说这期没有头条，就跳过，第一条放行业新闻。`公司头条` 不在领域池里、不参与轮换。

### 3. 条数与字数封顶

- 板块总数：`2–4 条` → **最多 5 条（含头条）**，常用 3–4 条。
- 每条正文 ≤250 字**不变**，仍用 `count_chars.py` 核。

### 4. 业务边界写死（补上漏掉的一条业务线）

- 原口径只有「儿童用品/童装 + 跨境」，**漏了「本地线下母婴集合店」**。现在写明三条业务线：
  ① 国内电商（抖音/天猫/拼多多，京东/唯品会次要）② 本地线下母婴集合店（门店零售、会员、即时零售）
  ③ 欧美跨境亚马逊（主推儿童睡袋）。品类限**婴童纺织用品 + 童装**。
- `assets/topics.json` 新增 `_business` 段（业务线 / 品类 / 不写品类 / 头条规则），
  `topic_menu.py` 每次运行都会把这它打印出来；`--list-all` 与 `--json` 也带上。
- 「渠道与私域」领域补上线下母婴集合店、门店经营、门店数字化的关键词。
- `content-guide.md` 第二节重写为「业务与品类范围」，第一、七节重写为写死的硬约束与新流程；
  `content.example.json` 第一条改成 `公司头条` 示例。

### 5. 无人值守（定时任务）的配套规则

- 刊期**自己算**（不再询问）；若报日期冲突则**停手报告**，不硬出图。
- **跳过头条**，交付时说明「头条位留空，需要补请告诉我」。
- 领域仍按 `topic_menu.py` 推荐 top 4 走，不要退回旧三件套。

**想回到旧行为**：删掉路径 B 的第 0、1 步即可（期号写死在 content.json、第一条放行业新闻），
其余不变。

## v1.3.0 — 2026-09-15

**内容不再框死：新增「选题」环节，先定领域再定向搜新闻**

孙哥反馈：内容框得有点死，希望由技能主动问几个相关领域、再去搜，而不是一上来就往
固定框架里填。问题出在三处 —— `content-guide.md` 写着 5 条固定检索线、立意举例写死、
`content.example.json` 里三条 topic 直接就是「电商大促/平台动向/跨境电商」，
结果期期都长一个样。

- **新增 `assets/topics.json`**：14 个领域的选题池（主题词 / 说明 / 检索关键词 / 来源 / 当季月份 / 权重）
- **新增 `scripts/topic_menu.py`**：扫项目目录里的历史 `*_content.json`，算出每个领域最近一次被用的期号，
  把「上期刚用过的」剔出推荐，输出本期候选（默认 4 个）。支持 `--list-all` / `--json` / `--count`
- **新增 `references/topic-menu.md`**：选题流程、AskUserQuestion 问法、无人值守策略、轮换规则、评分逻辑
- `content-guide.md` 第六节的 5 条固定检索线**撤掉**，改为「按当期选定领域的 keywords 搜」；
  第四节立意举例标注为「仅风格参考，不是固定模板」；条数由「3 条」放宽为 **2–4 条**
- `SKILL.md` 路径 B 由 6 步变 7 步，开头插入「选题」；文件地图、坑位清单、frontmatter description 同步更新
- `README.md` 加入「内容从哪来」说明；`content.example.json` 注明 topic 必须用领域池原词
  （换成别的说法，轮换统计就认不出来，下期会被当成「没用过」重复推荐）
- 每周五定时任务 prompt 同步：无人值守时按脚本推荐 top 4 走，交付时须说明选取理由并留回退口

轮换评分：`weight +（从未用过 60，或 min(50, 间隔期数×15)）+（当季 25）-（上期刚用 80）`，
按分降序取前 N。五个常量都在 `topic_menu.py` 顶部，想调手感改那里。

想回到固定选题：把 `topic-menu.md` 第一步跳过，直接按 `content-guide.md` 第五节的主题词硬写即可，
其余流程不变。

**顺带修掉**：`39_content.json` 的 `issue` / `date` 写成了 `38` / `2026.9.18`（成品实为第 39 期 / 9.25），
已改正 —— 这两个字段错了会让轮换统计把两期当成同一期，也会影响日后拿它重出图时的报头。

## v1.2.0 — 2026-09-15

**报头铺满顶部与左右到边（苔藓绿 banner 占满整个顶部）**

孙哥反馈：钉钉文档时代的版式里绿块四周留白，绿色没有占满顶部。现在改成整条铺满。

- `layout.json` 新增 `strip.enabled`（**false**）：顶部粉蓝渐变细条整条不再渲染
- `brandBar.marginX`：`60` → **`0`**，绿块左右铺满到边
- `nav.paddingX`：`30` → **`90`**。旧版的导航词对齐是「绿块留白 60 + paddingX 30」叠出来的 90px，
  铺满后那 60 没了，得直接写 90，导航词才能继续跟日期、正文左对齐成一条竖线
- `template.html`：新增 `{{STRIP_HTML}}` 占位符，细条那条 div 由 `build.py` 决定输不输出
- `build.py` 的 `verify()` 重写：行号改为**相对报头绿块顶部**计算，并新增 4 项报头几何断言
  （绿块顶部行 / 高 / 左边缘 / 右边缘）。以后切换报头版式都不用再改校验基准
- `baseline` 同步改版：`brandBarRows` → `barHeight`，新增 `titleWindow` / `navWindow` / `datelineWindow`
- 报头**内部**比例未动：白字距绿块顶 30px、距底 13~17px，与原版一致，观感不会变紧

想回到旧版报头：`strip.enabled` → `true`、`brandBar.marginX` → `60`、`nav.paddingX` → `30`。

## v1.1.1 — 2026-09-15

**技能改名为 `wellber-weekly`**

- 目录 `weili-weekly-briefing` → `wellber-weekly`，`SKILL.md` 的 frontmatter `name` 同步更新
  （两者必须一致，否则技能无法被正确识别）
- 更新所有引用：`SKILL.md` 文件地图、`README.md` 的 clone 示例、项目记忆、每周五定时任务
- 脚本无需改动：`build.py` / `check_env.py` 均用 `Path(__file__).parent.parent` 定位技能根目录，不依赖目录名
- 克隆目录名随之变为 `wellber-weekly`：`git clone https://github.com/ysunwellber/wellber-weekly.git wellber-weekly`

## v1.1 — 2026-09-15

**页尾改造：品牌落款取代二维码**

- 页尾从「170px 留白」改为品牌 logo 落款（`assets/logo.png`，wellber 威尔贝鲁，透明 PNG 已裁掉四周透明边）
- 新增 logo 两侧的浅橄榄分割线，与页眉绿块 `#b2b382` 同色系呼应（`footer.rule.mode = sides`）
- logo 尺寸定为 240px 稿宽（占版宽 18.8%），居中；高度按原图比例自适应，不写死
- 页脚全部参数化：`footer.logo`（src / width / opacity / marginTop / alt）+ `footer.rule`（mode / length / thickness / gap / color）+ `footer.bottomGap`
- `build.py` 新增 logo 素材自动接管：技能自带 logo，使用者无需自备素材

**修掉的坑**

- `footer.bottomGap` 语义修正为「logo 下方留白」。v1.0 版本曾把它误当容器总高，导致 logo 下方只剩一半留白

**文档**

- `references/layout-spec.md` 第六节整节重写，含换 logo / 调大小 / 改线型的操作说明
- `README.md` 版式示例与 Q&A 同步为 footer 结构

## v1.0 — 2026-09-15

**首版：版式与内容解耦**

- 逆向还原「威尔新资讯」周报长图版式（1744px 宽），所有参数抽到 `assets/layout.json` 单一来源
- `assets/template.html` 全部改用 CSS 变量，不写死数值
- `scripts/build.py`：内容 JSON 进，整版长图 / 微信分片 / PDF 出，一条命令完成
- 内置版式回归校验（绿块行范围、大标题 ink 高宽、导航词行带、日期行带）5 项自动比对基准值
- `scripts/split_long_image.py`：在真·空白行切分微信分片，避免切断文字行
- `scripts/count_chars.py`：正文段落字数核验（硬约束：每条 ≤250 字含标点）
- `scripts/check_env.py`：环境自检
- 跨平台：Windows / macOS / Linux 三套中文字体栈；弃用 `file://` 改本地 HTTP 渲染以绕开字体 CORS；浏览器回退链 chromium → Chrome → Edge；仅依赖 Pillow + Playwright（已去 numpy 依赖）
